[package]
name = "wayfolio-wasm"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wayfolio = { package = "wayfolio-core", path = "../core" }
wasm-bindgen = "0.2.84"
thiserror = "2.0.18"
error_reporter = "1.0.0"

[profile.release]
opt-level = "s"
